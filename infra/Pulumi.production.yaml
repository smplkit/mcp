config:
  aws:region: us-east-1
  # Scenario B: served from the consolidated single-host platform.
  mcp:cutoverToHost: "true"
  mcp:oauthAuthkitDomain: https://satisfying-voyage-87-staging.authkit.app
