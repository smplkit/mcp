# A long random string. Set the same value as the X-Job-Secret header on the
# smplkit job, and as this env var on your deployment. The endpoint returns 401
# unless they match.
JOB_SECRET=change-me-to-a-long-random-string
